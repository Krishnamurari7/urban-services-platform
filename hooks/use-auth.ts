"use client";

/**
 * @deprecated Use useAuth from @/components/auth/auth-provider instead
 * This hook is kept for backward compatibility but will be removed in the future.
 */
export { useAuth } from "@/components/auth/auth-provider";
